export type Certification = {
    title: string;
    issuer: string;
    date: string;
    credentialUrl?: string;
    image?: string;
};

export const certifications: Certification[] = [
    //TODO
];
